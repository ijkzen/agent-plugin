{
  "description": "agent-plugin hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh ${CLAUDE_PLUGIN_ROOT}/scripts/codegraph-sync.sh"
          }
        ]
      }
    ]
  }
}
